#ifndef RAY_TRACER_H
#define RAY_TRACER_H


class ray_tracer
{
    public:
        ray_tracer();
        virtual ~ray_tracer();

    protected:

    private:
};

#endif // RAY_TRACER_H
